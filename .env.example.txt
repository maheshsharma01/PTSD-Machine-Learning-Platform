# Environment name (development, staging, production)
ENVIRONMENT=development

# Secret key for session and encryption (set a strong random string)
SECRET_KEY=your-secure-random-secret-key

# Database connection URL (PostgreSQL example)
DATABASE_URL=postgresql://username:password@localhost:5432/ptsd_ml_db

# Redis URL (if using Redis caching or session store)
REDIS_URL=redis://localhost:6379/0

# ML model storage path
MODEL_STORAGE_PATH=/path/to/models

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Flask/Streamlit port (if configurable)
APP_PORT=8501

# Mail server settings (if email notifications are used)
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_USE_TLS=True

# Other API keys or third-party service tokens
EXTERNAL_API_KEY=your-api-key-here
